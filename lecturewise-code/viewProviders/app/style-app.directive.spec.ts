import { StyleAppDirective } from './style-app.directive';

describe('StyleAppDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleAppDirective();
    expect(directive).toBeTruthy();
  });
});
